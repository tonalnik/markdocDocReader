{
	"name": "doc-reader",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"lint": "next lint",
		"build": "next build",
		"prune": "node-prune",
		"dev": "next dev -p ${PORT}",
		"start": "next start -p ${PORT}",
		"analyze": "cross-env ANALYZE=true next build",
		"test": "jest --ci --reporters=default --reporters=jest-junit",
		"build:fn-schemes": "node ./logic/buildFnSchemes.js"
	},
	"dependencies": {
		"@markdoc/markdoc": "^0.1.2",
		"next": "^12.1.6"
	},
	"devDependencies": {
		"@types/node": "^17.0.34",
		"@types/react": "^18.0.9",
		"typescript": "^4.6.4"
	}
}
